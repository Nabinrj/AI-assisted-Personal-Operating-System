import type {Analytics} from "../types";
export function AnalyticsPage({data}:{data:Analytics|null}){if(!data)return <section className="card"><h2>Analytics</h2><p className="muted">No analytics data available yet.</p></section>;return <><div className="grid"><div className="card metric"><span>Execution</span><strong>{data.execution_rate}%</strong></div><div className="card metric"><span>Tasks</span><strong>{data.tasks_completed}/{data.tasks_total}</strong></div><div className="card metric"><span>Active goals</span><strong>{data.goals_active}</strong></div><div className="card metric"><span>Active projects</span><strong>{data.projects_active}</strong></div><div className="card metric"><span>Income</span><strong>NPR {data.income_total}</strong></div></div><section className="card list"><h2>Time by category</h2>{data.time_by_category.length?data.time_by_category.map(x=><div className="item" key={x.category}><b>{x.category}</b><small>{x.minutes} min</small></div>):<p className="empty">No time has been logged yet.</p>}</section></>}
