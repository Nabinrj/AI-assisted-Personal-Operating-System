import {CreateForm} from "../components/CreateForm";import {EntityList} from "../components/EntityList";import {request} from "../lib/api";import type {Project} from "../types";
export function ProjectsPage({items,token,reload}:{items:Project[];token:string;reload:()=>Promise<void>}){return <><CreateForm label="project" onCreate={async title=>{await request("/api/v1/projects",token,{method:"POST",body:JSON.stringify({title,status:"active",objective:"",next_action:"",blocker:""})});await reload()}}/><EntityList title="Projects" empty="No projects yet. Define the outcome and next action.">{items.map(p=><div className="item" key={p.id}><b>{p.title}</b><small>{p.status} · Next: {p.next_action||"not defined"}{p.blocker?` · Blocker: ${p.blocker}`:""}</small></div>)}</EntityList></>}
