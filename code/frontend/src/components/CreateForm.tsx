import {FormEvent,useState} from "react";
export function CreateForm({label,onCreate}:{label:string;onCreate:(title:string)=>Promise<void>}){const [title,setTitle]=useState("");const [busy,setBusy]=useState(false);async function submit(e:FormEvent){e.preventDefault();if(!title.trim())return;setBusy(true);try{await onCreate(title.trim());setTitle("")}finally{setBusy(false)}}return <form className="create-form" onSubmit={submit}><input value={title} onChange={e=>setTitle(e.target.value)} placeholder={"New "+label}/><button className="primary" disabled={busy}>{busy?"Adding…":"Add"}</button></form>}